# Entrada de dados
palavra = input()

# Processamento
palavra_comeco = palavra[0:2]
palavra_final = palavra[-2] + palavra[-1]
palavra_total = palavra_comeco + palavra_final

# Saída de dados
print(palavra_total)